<section class="hidden-print">
  <h1 class="page-header">
   <i class="fa fa-trophy"></i> <%=t ( '.page_header') %>
</h1>
</section>
<div class="col-lg-12">
  <div ng-repeat="mem in get_members track by $index" class="panel panel-default">
  	{{mem.id}}, {{mem.name}}
  </div>
</div>