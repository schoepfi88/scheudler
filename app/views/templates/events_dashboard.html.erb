<section class="hidden-print">
  <h1 class="page-header">
   <i class="fa fa-trophy"></i> <%= @event.name + " - " + t('.page_header') %>
</h1>
</section>

<ul class="col-lg-12 list-group">
	<li class="list-group-item" ng-repeat="mem in get_members track by $index">
		{{mem.name}}
	</li>
</ul>
