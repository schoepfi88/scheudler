<section class="hidden-print">
  	<h1 class="page-header">
		<i class="fa <%= @group.icon %>"></i> 
		<%= @group.name + t(".page_header") %>
		<% if @is_admin %>
			<div class="btn-group pull-right">
			  <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
				<i class="fa fa-gear"></i> Settings<span class="caret"></span>
			  </button>
			  <ul class="dropdown-menu" role="menu">
				<li><a href="#"><i class="fa fa-plus"></i> Invite People</a></li>
				<li><a href="#">Something else here</a></li>
				<li class="divider"></li>
				<li><a href="#"><i class="fa fa-gears"></i> Group Settings</a></li>
			  </ul>
			</div>
			
		<% end %>
	</h1>
</section>
<% if @is_member %>

	<%= "You are a member" %>

<% else %>
	<%= "You are no member of this group" %>
<% end %>
